services:
  nexus:
    build: .
    container_name: nexus
    ports:
      - "8083:8081"
    volumes:
      - ./data:/nexus-data
    networks:
      - nexus-net

networks:
  nexus-net:
    driver: bridge
